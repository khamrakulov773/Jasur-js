{
  "rules": {
    "todos": {
      "v1": {
        "$uid": {
          ".read": "$uid === auth.uid",  // Разрешить чтение только владельцу данных
          ".write": "$uid === auth.uid"  // Разрешить запись только владельцу данных
        }
      }
    }
  }
}
